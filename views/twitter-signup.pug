extends site-layout.pug
block content
    link(rel="stylesheet", href="twitter-signup.css")
    form(action="/twitter-clone/signup",method="post")
        .signup
            h1 Sign up
            hr
            .username
                label(for="username") #[b Username]
                input(type="text",placeholder="Enter username",name="username",required,autofocus)
            .password
                label(for="password") #[b Password]
                input(type="password",placeholder="Enter password",name="password",required)
            button(type="submit")#signup-btn Sign up
    .login
        p Already have an account? #[a(href="/twitter-clone/login") Log in]!